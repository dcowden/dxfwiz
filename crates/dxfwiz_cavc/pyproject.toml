[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "dxfwiz-cavc"
version = "0.1.0"
description = "Cavalier Contours native geometry adapter for dxfwiz."
requires-python = ">=3.13"

[tool.maturin]
bindings = "pyo3"
features = ["pyo3/extension-module"]
