# Nesting Fixtures

These fixtures are split out of human-packed integration DXFs. Each case keeps
a copy of the source drawing plus one DXF per detected part under `parts/`.

Generated test artifacts should go under `tests/output/nesting/<test name>/`.
